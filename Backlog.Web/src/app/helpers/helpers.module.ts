import { NgModule } from '@angular/core';
import { HttpModule } from "@angular/http";

const providers = [];

@NgModule({
    imports: [HttpModule],
	providers: providers
})
export class HelpersModule { }
